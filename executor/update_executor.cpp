#include "executor.h"

namespace uhp_sql {

std::string Executor::ExecUpdateStatement(const hsql::UpdateStatement* stmt,
                                          uintmax_t numIndent) {}
}  // namespace uhp_sql
